---
import { Image } from 'astro:assets';

interface Post {
  title: string;
  body: string;
  image: string;
  tags: string[];
}

interface Props {
  title: string;
  posts: Post[];
  button: string;
}

const { title, posts, button } = Astro.props;
---

<div class="xl:min-h-[788px] h-fit bg-gradient-to-b from-sky-100 to-sky-200  flex items-start justify-center">
  <div class="w-11/12 lg:w-2/3 flex flex-col justify-center gap-y-10 mb-10 xl:mb-20">
    <div class="flex flex-row items-center justify-start h-full w-full xl:w-[70%] gap-x-6">
      <h1 class="text-2xl xl:text-3xl 2xl:text-4xl font-bold text-start text-gray-600 opacity-70">{title}</h1>
      <Image src="/latest_blog/coming.svg" alt="Coming soon" width={30} height={30} class="w-10"/> 
    </div>
    <div class="flex flex-col justify-center items-center gap-y-10">
      <div class="grid grid-cols-1 md:grid-cols-2 2lg:grid-cols-3 gap-6">
        {posts.map((post, index) => (
          <div class="flex flex-col bg-white rounded-[50px] overflow-hidden flex-shrink-0 opacity-70">
              <div class="w-full h-40 xl:h-72 overflow-hidden">
                  <Image src={`${post.image}`} alt="" width={30} height={30} class="w-full h-full object-cover"/> 
              </div>
              <div class="p-8 xl:p-10 flex flex-col gap-y-2 xl:gap-y-4">
                  <div class="flex flex-row gap-2 w-full flex-wrap">
                      {post.tags.map((tag, index) => (
                          <div class="px-2.5 py-0.5 bg-sky-100 rounded-[7px] justify-center items-center ">
                              <div class="text-blue-500 text-[10px] font-medium leading-[13px]">{tag}</div>
                          </div>
                      ))}
                  </div>
                  <h1 class="text-blue-500 text-xs xl:text-base font-bold">{post.title}</h1>
                  <p class="text-gray-400 text-2xs xl:text-sm ">{post.body}</p>
              </div>
          </div>
      ))}
      
      </div>
      <!-- <div class="h-11 px-6 py-2.5 bg-white rounded-2xl flex justify-center items-center cursor-pointer transform transition-transform duration-300 hover:scale-105 hover:shadow-lg">
        <div class="text-blue-500 text-xs font-extrabold uppercase">{button}</div>
      </div> -->
      <div class="h-11 px-6 py-2.5 bg-white rounded-2xl flex justify-center items-center opacity-70">
        <div class="text-blue-500 text-xs font-extrabold uppercase">{button}</div>
      </div>
    </div>
  </div>
</div>
